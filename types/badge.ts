export type ResponseBadge = {
  badgeType: string
  badgeTitle: string
  description: string
  acquired: boolean
  currentCount: number
  requiredCount: number
  remainingCount: number
  badgeImageUrl: string
}
